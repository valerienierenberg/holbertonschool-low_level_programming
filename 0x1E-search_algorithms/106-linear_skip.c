#include "search_algos.h"
/**
* linear_skip- searches for a value in a sorted array of integers using
* the Binary search algorithm
* @list: pointer to the first element of the linked list to search in
* @value: value to search for
* Return: the FIRST index where value is located
* Using recursion
* If value is not present in array or if array is NULL, function returns -1
*/
skiplist_t *linear_skip(skiplist_t *list, int value)
{
;
}
